# FCM API 서버용 Dockerfile
FROM golang:1.23-alpine AS builder

WORKDIR /app

# Go modules 복사 및 다운로드
COPY go.mod go.sum ./
RUN go mod download

# 소스 코드 복사
COPY . .

# API 서버 빌드
RUN CGO_ENABLED=0 GOOS=linux go build -o fcm-api ./cmd/api

# 실행 이미지
FROM alpine:latest

# 필수 패키지 설치
RUN apk --no-cache add ca-certificates tzdata

WORKDIR /root/

# 빌드된 바이너리 복사
COPY --from=builder /app/fcm-api .

# 포트 노출
EXPOSE 8080

# 실행
CMD ["./fcm-api"]